version: '3.8'

services:
  web:
    build: .
    container_name: erpnext_portfolio
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=production
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    command: python app.py
